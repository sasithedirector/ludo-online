{
  "version": "1.0.0",
  "description": "Online Multiplayer Ludo Game — Production",
  "scripts": {
    "install": "cd server && npm ci --production && cd ../client && npm ci && npm run build",
    "start": "cd server && NODE_ENV=production node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
